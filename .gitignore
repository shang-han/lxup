# ── 运行时数据（数据库 / agent workspace / 日志）──
runtime/

# ── 上游引擎参考源码（体量大，非 LXUP 自研；如需入库请改用 git submodule）──
engines/

# ── 依赖与构建产物 ──
node_modules/
dist/
__pycache__/
*.py[cod]
*.egg-info/

# ── 日志与临时文件 ──
*.log
.sidecar.ready
.gateway.ready

# ── 环境变量与密钥 ──
.env
*.pem
*.key

# ── 编辑器 / 系统 ──
.DS_Store
Thumbs.db
.idea/
.vscode/
